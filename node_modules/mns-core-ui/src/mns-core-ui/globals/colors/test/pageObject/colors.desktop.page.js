import Page from '../../../../../test/pageObject/page';

const DEFAULT_PATH = 'examples/globals/colors/body';

export default class ColorsDesktopPage extends Page {

    constructor(path = DEFAULT_PATH) {
        super(path);
    }

    open() {
        super.open();
        this.desktopViewport();
    }

}
