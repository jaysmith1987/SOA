import Page from '../../../../../test/pageObject/page';

const DEFAULT_PATH = 'examples/globals/typography/body';

export default class TypographyPage extends Page {

    constructor(path = DEFAULT_PATH) {
        super(path);
    }

    open() {
        super.open();
        this.desktopViewport();
    }
}
