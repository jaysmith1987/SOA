import Page from '../../../../../test/pageObject/page';

const DEFAULT_PATH = 'examples/globals/icons/body';

export default class IconsMobilePage extends Page {

    constructor(path = DEFAULT_PATH) {
        super(path);
    }

    open() {
        super.open();
        this.mobileViewport();
    }

}
