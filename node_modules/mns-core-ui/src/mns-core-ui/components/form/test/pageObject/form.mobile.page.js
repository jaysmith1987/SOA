import Page from '../../../../../test/pageObject/page';

const DEFAULT_PATH = 'examples/components/form/body';

export default class FormMobilePage extends Page {

    constructor(path = DEFAULT_PATH) {
        super(path);
    }

    open() {
        super.open();
        this.mobileViewport();
    }

}
