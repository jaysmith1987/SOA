import Page from '../../../../../test/pageObject/page';

const DEFAULT_PATH = 'examples/components/buttons/body';

export default class ButtonsDesktopPage extends Page {

    constructor(path = DEFAULT_PATH) {
        super(path);
    }

    open() {
        super.open();
        this.desktopViewport();
    }
}
