import ButtonsDesktopPage from './pageObject/buttons.page';

describe('Components : Buttons', () => {

    let buttonsDesktopPage = new ButtonsDesktopPage();

    before(()=> {
        buttonsDesktopPage.open();
    });

    describe('Desktop', () => {

        it('Buttons example', () => {

            var result = browser.webdrivercss('template__navigation--desktop', [
                {
                    name: 'buttons-example',
                    elem: 'body',
                    screenWidth: [breakpoint.xlarge.width, breakpoint.large.width]
                }
            ]);

            isWithinMisMatchTolerance(result);
        });
    });
});
