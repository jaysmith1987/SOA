import Page from '../../../../../../test/pageObject/page';

const DEFAULT_PATH = 'examples/templates/footer/footer.html';

export default class FooterPage extends Page {

    constructor(path = DEFAULT_PATH) {
        super(path);
    }

    open() {
        super.open();
    }
}
